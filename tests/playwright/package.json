{
  "name": "browser-benchmark",
  "version": "1.0.0",
  "description": "Browser-level benchmark: REST vs gRPC-Web via real Chromium instances",
  "scripts": {
    "test": "node browser-benchmark.js",
    "install-browser": "npx playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.52.0"
  }
}
